# Custom .bin Builds

Flashable CrossPoint firmware images built from this repository's Anki patch.

Work in progress: the CrossPoint **1.6.0rc** based X4/X3 build is being prepared
here. This file is replaced with the build instructions, the patch, and the
`.bin` + `SHA256SUMS` once the build completes.

Upstream base: <https://github.com/crosspoint-reader/crosspoint-reader> (tag `1.6.0rc`)
